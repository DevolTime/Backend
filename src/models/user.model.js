import {  mongoose, Schema} from "mongoose";

// 1era parte para definir el esquema 

const userShema = new Schema({},{});

//2da parte definir el modelo 
const userShema = model ();


export default userShema;